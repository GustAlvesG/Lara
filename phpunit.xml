<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            NÃO COMENTE ESTAS DUAS LINHAS.

            Os testes usam RefreshDatabase, que APAGA todas as tabelas do banco
            configurado. Sem este override, `php artisan test` roda contra o
            DB_CONNECTION do .env — o banco de homologação — e destrói os dados.
            Já aconteceu duas vezes.

            Isto cobre o caso normal, mas não é a única proteção: uma variável
            DB_CONNECTION exportada no shell ainda vence o que está aqui (nem
            `force="true"` resolve). Por isso Tests\TestCase aborta a suíte
            quando o driver não é SQLite — é essa trava que realmente impede o
            estrago. Veja tests/Unit/DatabaseIsolationTest.php.
        -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
